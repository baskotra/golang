package main

import "fmt"

func main() {
	// q := []int{1, 2, 3, 4}
	// fmt.Println(len(q), cap(q))

	a := make([]int, 5)
	// b := make([]int, 0, 10) //* Len = 0 and Cap = 10
	fmt.Println(a)
	// fmt.Println(b)

	// a = append(a, 5)
	// fmt.Println(len(a))

	//* Range function use
	// str_arr := []string{"a", "b", "c", "d", "e"}
	// for i, c := range str_arr {
	// 	fmt.Println(c, " is on index ", i)
}
